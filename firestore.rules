rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Globally allow all reads and writes for the prototype phase.
    // This removes all permission blocks while you build and test.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}